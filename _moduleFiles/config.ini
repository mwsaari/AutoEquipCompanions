# AutoEquipCompanions configuration
# Reload the game for changes to take effect.

# Use BehaviorV2 for auto-equipping (no Harmony required).
# Set to false to use the original Harmony-based behavior.
UseV2 = true
